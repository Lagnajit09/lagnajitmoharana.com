# Copy to .env.local and fill in after deploying the Cloudflare Worker.
# Vite exposes VITE_* variables to the browser bundle.
VITE_CONTACT_WORKER_URL=https://contact-worker.<your-subdomain>.workers.dev
